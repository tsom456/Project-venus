import React from "react";
import ReactDOM from "react-dom/client";
import SourcingHub from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SourcingHub />
  </React.StrictMode>
);
