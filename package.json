{
  "name": "industrials-sourcing-hub",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "smoke": "esbuild scripts/smoke.jsx --bundle --format=esm --platform=node --packages=external --jsx=automatic --outfile=node_modules/.smoke/smoke.mjs && node node_modules/.smoke/smoke.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "esbuild": "^0.27.2",
    "vite": "^6.0.0"
  }
}
